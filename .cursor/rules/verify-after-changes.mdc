---
description: 代码改动后必须先自行验证，再回复用户
alwaysApply: true
---

# 改动后自测

每次完成代码修改后，**在回复用户之前**必须自行验证，不得仅凭静态分析宣称完成。

## 必做检查（按改动范围选取）

1. **单元测试**：`npm test`（项目有测试时）
2. **构建**：`npm run build`（涉及页面/组件/类型时）
3. **页面/UI**：启动或复用 dev server，用浏览器访问受影响路由，确认 DOM 与交互符合预期

## UI 改动额外要求

- 只改用户点名的节点，勿顺带删除或调整无关样式/结构
- 验证时对照用户要求逐项确认（例如：删了 header 但 hero 光晕仍在）

## 例外

用户在本对话明确说「我自己测 / 自行验收 / 改完不用你测」时：不要跑测试、build、浏览器联调；改完只说明改动点，把验收交给用户。未声明时仍按上文必做检查。

## 回复格式

向用户汇报时简要说明验证项与结果（通过/失败及现象），失败则先修复再回复。用户自行验收时，省略验证项。
